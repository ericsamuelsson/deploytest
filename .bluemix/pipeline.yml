---
stages:
- name: esa build
  inputs:
  - type: git
    branch: master
  triggers:
  - type: commit
  jobs:
  - name: build
    type: builder
    build_type: shell
    script: |-
      #!/bin/bash
      # your script here
      echo test
- name: esa deploy
  inputs:
  - type: job
    stage: esa build
    job: Build
  triggers:
  - type: stage
  jobs:
  - name: deploy
    type: deployer
    target:
      url: https://api.ng.bluemix.net
      organization: eric.samuelsson@se.ibm.com
      space: dev
      application: deploytest
    script: |
      #!/bin/bash
      echo YYY
